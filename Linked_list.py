# Linked list in python
class Node:
    def __init__(self, data):
        self.data = data
        self.next = none
    
        # creating node
        node1 = Node(10)
        node2 = Node(20)
        node3 = Node(30)
        node4 = Node(30)
    
        # connecting a data in form of linked list
        node.next = node2
        node.next = node3
        node.next = node4
    
       # printing the list
        head = node1
        current = head
        while current is not none:
            print("current.data,end" "->")
        current = current.next
        print("None")
        
        
        